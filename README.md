## 📌 Описание

Этот Telegram-бот предназначен для мониторинга MQTT-топиков с возможностью настройки оповещений, клавиатур и инлайн-кнопок. Бот позволяет:

- Получать уведомления о важных событиях в реальном времени.
- Управлять устройствами через инлайн-кнопки.
- Настраивать динамические сообщения и клавиатуры на основе данных из MQTT.
- Реализовать сложные правила блокировки и фильтрации уведомлений.

Проект идеально подходит для систем автоматизации, IoT-устройств и мониторинга.

---

## 🛠 Требования

- Node.js (версия 12 или выше)
- Доступ к MQTT-брокеру
- Telegram Bot Token
- Разрешенные chat_id пользователей

---

## 🚀 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/FirstTact/tj-mqtt-telegram.git
   cd tj-mqtt-telegram
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл конфигурации:**
   Создайте файл `newShema.json` в корневой директории проекта. Пример структуры файла см. в разделе [Конфигурация](#⚙️-конфигурация).

4. **Запустите бота:**
   ```bash
   node index.js
   ```

---

## ⚙️ Конфигурация

Файл `newShema.json` содержит все необходимые настройки для работы бота. Пример базовой структуры:

```json
{
  "botKey": "YOUR_TELEGRAM_BOT_TOKEN",
  "ip": "MQTT_BROKER_IP",
  "clients": [CHAT_ID_1, CHAT_ID_2],
  "warning": [],
  "keyboards": [],
  "inline_buttons": []
}
```

### Подробное описание секций:

### Подробное описание структуры `warning`

Секция `warning` в конфигурации используется для настройки системы оповещений. Она позволяет боту реагировать на изменения значений в MQTT-топиках и отправлять уведомления пользователям с учетом различных условий. Рассмотрим каждый параметр на примере вашего конфига:

---

#### **Пример конфигурации:**
```json
"warning": [
  {
    "id": 1,
    "topic": "/devices/wb-mwac-v2_11/controls/Input P1",
    "message": [
      {
        "value": 1,
        "toStart": true,
        "text": "⚠️ *Обнаружена протечка!*\n📍 Местоположение: Санузел при гостевой спальне",
        "block": [
          {
            "topic": "/devices/wb-mwac-v2_11/controls/Cleaning Mode",
            "value": 1
          }
        ]
      }
    ],
    "inline_button_id": [1]
  }
]
```

---

### **Параметры `warning`:**

#### 1. **`id`**
- **Тип:** Число (целое)
- **Описание:** Уникальный идентификатор предупреждения. Используется для внутренней идентификации и может быть полезен для отладки или расширения функционала.
- **Пример:** `"id": 1`

---

#### 2. **`topic`**
- **Тип:** Строка
- **Описание:** MQTT-топик, который будет мониториться. Бот подписывается на этот топик и следит за изменениями его значений.
- **Пример:** `"topic": "/devices/wb-mwac-v2_11/controls/Input P1"`

---

#### 3. **`message`**
- **Тип:** Массив объектов
- **Описание:** Определяет правила обработки значений из топика и текстовые сообщения для отправки пользователям.
- **Поля объекта внутри массива:**

  - **`value`**
    - **Тип:** Число или строка
    - **Описание:** Значение топика, при котором будет срабатывать правило.
    - **Пример:** `"value": 1`

  - **`toStart`**
    - **Тип:** Логическое значение (`true`/`false`)
    - **Описание:** Флаг, указывающий, нужно ли отправлять это уведомление сразу после запуска бота (если значение топика уже соответствует указанному). Если `false`, уведомление игнорируется при начальной загрузке.
    - **Пример:** `"toStart": true`

  - **`text`**
    - **Тип:** Строка
    - **Описание:** Текст уведомления, который будет отправлен пользователю. Может содержать Markdown-форматирование и поддерживает динамические значения через `${value}`.
    - **Пример:** `"text": "⚠️ *Обнаружена протечка!*\n📍 Местоположение: Санузел при гостевой спальне"`

  - **`block`**
    - **Тип:** Массив объектов
    - **Описание:** Правила блокировки уведомлений. Если хотя бы одно правило выполняется, уведомление не отправляется.
    - **Поля объекта внутри массива:**
      - **`topic`**
        - **Тип:** Строка
        - **Описание:** MQTT-топик, который проверяется на соответствие условию.
        - **Пример:** `"topic": "/devices/wb-mwac-v2_11/controls/Cleaning Mode"`
      - **`value`**
        - **Тип:** Число или строка
        - **Описание:** Значение топика, которое должно блокировать отправку уведомления.
        - **Пример:** `"value": 1`

---

#### 4. **`inline_button_id`**
- **Тип:** Массив чисел
- **Описание:** Список идентификаторов инлайн-кнопок, которые будут добавлены к уведомлению. Каждый `id` должен соответствовать кнопке, описанной в секции `inline_buttons` конфигурации.
- **Пример:** `"inline_button_id": [1]`

---

### **Как это работает?**

1. **Подписка на топик:**
   - Бот подписывается на топик `/devices/wb-mwac-v2_11/controls/Input P1`.
   - При изменении значения в этом топике бот проверяет, соответствует ли новое значение одному из правил в массиве `message`.

2. **Проверка условия (`value`):**
   - Если значение топика становится равным `1`, бот находит соответствующее правило:
     ```json
     {
       "value": 1,
       "toStart": true,
       "text": "⚠️ *Обнаружена протечка!*\n📍 Местоположение: Санузел при гостевой спальне",
       "block": [
         {
           "topic": "/devices/wb-mwac-v2_11/controls/Cleaning Mode",
           "value": 1
         }
       ]
     }
     ```

3. **Проверка блокировок (`block`):**
   - Бот проверяет значение топика `/devices/wb-mwac-v2_11/controls/Cleaning Mode`.
   - Если значение равно `1`, уведомление блокируется.

4. **Отправка уведомления:**
   - Если блокировка не сработала, бот отправляет уведомление:
     ```
     ⚠️ *Обнаружена протечка!*
     📍 Местоположение: Санузел при гостевой спальне
     ```
   - Если указан `inline_button_id`, к уведомлению добавляются инлайн-кнопки.

---

### **Пример использования `block`:**

Если режим очистки (`Cleaning Mode`) активен (значение `1`), уведомление о протечке не отправляется. Это полезно, например, если система находится в тестовом режиме или обслуживании.

---

### **Пример использования `toStart`:**

Если `toStart: true`, бот отправит уведомление сразу после запуска, если значение топика уже соответствует условию. Например, если при запуске бота топик `/devices/wb-mwac-v2_11/controls/Input P1` имеет значение `1`, уведомление будет отправлено немедленно.

---

### **Пример использования `inline_button_id`:**

Если указан массив `[1]`, к уведомлению добавится инлайн-кнопка с `id: 1`. Например, кнопка может выполнять команду "Отключить сигнал тревоги" или "Перейти в режим очистки".

---

### **Дополнительные возможности:**

1. **Множественные правила (`message`):**
   - Можно добавить несколько правил для одного топика. Например:
     ```json
     "message": [
       {
         "value": 1,
         "text": "⚠️ Протечка обнаружена!"
       },
       {
         "value": 0,
         "text": "✅ Протечка устранена."
       }
     ]
     ```

2. **Расширенные блокировки:**
   - Можно добавить несколько условий блокировки:
     ```json
     "block": [
       {
         "topic": "/devices/wb-mwac-v2_11/controls/Cleaning Mode",
         "value": 1
       },
       {
         "topic": "/devices/wb-mwac-v2_11/controls/Maintenance Mode",
         "value": "on"
       }
     ]
     ```
![image](https://github.com/user-attachments/assets/8ee5af0f-45b1-40b3-8376-7d272c1f8e17)

---



### **Раздел `keyboards`**

Раздел `keyboards` в конфигурации отвечает за создание интерактивного меню Telegram-бота. Он позволяет организовать удобную навигацию между разделами, выводить информацию о состоянии устройств и систем, а также предоставлять пользователю возможность управлять ими через кнопки.

---

### **Структура раздела `keyboards`**

Каждый объект в массиве `keyboards` представляет собой одну "страницу" меню (или список). Каждая страница содержит набор кнопок, которые могут выполнять следующие функции:
1. Переход на другую страницу меню.
2. Отображение информации о конкретном разделе или устройстве.
3. Управление устройствами через MQTT-топики.


#### **Основные параметры:**

1. **`list_id`**
   - **Тип:** Число
   - **Описание:** Уникальный идентификатор страницы меню. Используется для перехода между страницами.
   - **Пример:** `"list_id": 1`

2. **`buttons`**
   - **Тип:** Массив объектов
   - **Описание:** Список кнопок, доступных на данной странице. Каждая кнопка может быть связана с определенным действием или информацией.

---

### **Параметры кнопок (`buttons`)**

Каждая кнопка в массиве `buttons` имеет следующие параметры:

1. **`row`**
   - **Тип:** Число
   - **Описание:** Номер строки, в которой будет размещена кнопка. Кнопки с одинаковым значением `row` будут расположены в одной строке.
   - **Пример:** `"row": 1`

2. **`order`**
   - **Тип:** Число
   - **Описание:** Порядок кнопки в строке. Кнопки с меньшим значением `order` будут располагаться левее.
   - **Пример:** `"order": 1`

3. **`name`**
   - **Тип:** Строка
   - **Описание:** Текст, который будет отображаться на кнопке.
   - **Пример:** `"name": "💧 Защита от протечки"`

4. **`change_list`** *(опционально)*
   - **Тип:** Число
   - **Описание:** Если указано, кнопка будет перенаправлять пользователя на страницу меню с указанным `list_id`.
   - **Пример:** `"change_list": 2`

5. **`heading`** *(опционально)*
   - **Тип:** Строка
   - **Описание:** Заголовок, который будет отображаться при выборе кнопки. Обычно используется для описания раздела.
   - **Пример:** `"heading": "*💧 Защита от протечки*"`

6. **`strings`** *(опционально)*
   - **Тип:** Массив объектов
   - **Описание:** Список строк или данных, которые будут отображаться при выборе кнопки. Может содержать как статический текст, так и динамические значения из MQTT-топиков.
   - **Пример:**
     ```json
     "strings": [
       {
         "text": "Цоколь"
       },
       {
         "topic": "/devices/wb-mwac-v2_11/controls/Input F1",
         "message": [
           {
             "value": 0,
             "text": "📍 Санузел в цоколе: ✅ Датчик сухой"
           },
           {
             "value": 1,
             "text": "📍 Санузел в цоколе: ⚠️ Протечка"
           }
         ]
       }
     ]
     ```

7. **`inline_button_id`** *(опционально)*
   - **Тип:** Массив чисел
   - **Описание:** Список идентификаторов инлайн-кнопок, которые будут добавлены к сообщению. Каждый `id` должен соответствовать кнопке, описанной в секции `inline_buttons`.
   - **Пример:** `"inline_button_id": [2, 3]`

---

### **Поддерживаемые форматы данных в `strings`**

Каждый объект в массиве `strings` может иметь несколько форматов, в зависимости от типа данных:

1. **Простой текст**
   - **Параметр:** `text`
   - **Описание:** Статический текст, который будет отображаться без изменений.
   - **Пример:**
     ```json
     {
       "text": "Цоколь"
     }
     ```

2. **Динамическое значение из MQTT-топика**
   - **Параметр:** `topic`
   - **Описание:** MQTT-топик, из которого берется значение. Поддерживает различные форматы отображения:
     - **Массив сообщений по значению:**
       ```json
       {
         "topic": "/devices/wb-mwac-v2_11/controls/Input F1",
         "message": [
           {
             "value": 0,
             "text": "📍 Санузел в цоколе: ✅ Датчик сухой"
           },
           {
             "value": 1,
             "text": "📍 Санузел в цоколе: ⚠️ Протечка"
           }
         ]
       }
       ```
     - **on/off сообщения:**
       ```json
       {
         "topic": "/devices/wb-mwac-v2_11/controls/Cleaning Mode",
         "on_value": "🧹 Режим уборки включен",
         "off_value": "🧹 Режим уборки выключен"
       }
       ```
     - **Диапазон значений (normal_values и trigger_values):**
       ```json
       {
         "topic": "/devices/ABB_EC _1/controls/value",
         "values": [
           {
             "normal_values": {
               "min_value": 2,
               "max_value": 49,
               "text": "⚡️Текущие энергопотрение: ${value} кВт/ч"
             }
           },
           {
             "trigger_value": 50,
             "action": ">",
             "text": "⚠️ *Внимание, приближение к критическому значению потребления* (${value} кВт.ч)"
           }
         ]
       }
       ```

---

### **Пример использования**

#### **Простое меню с переходом между страницами**
```json
{
  "list_id": 1,
  "buttons": [
    {
      "row": 1,
      "order": 1,
      "name": "Лист 2",
      "change_list": 2
    },
    {
      "row": 1,
      "order": 2,
      "name": "Лист 3",
      "change_list": 3
    }
  ]
}
```
![image](https://github.com/user-attachments/assets/9ea20a7d-0f88-4f22-a16c-9783783c7a96)

#### **Информационная кнопка с динамическими данными**
```json
{
  "row": 1,
  "order": 1,
  "name": "⚡ Электрика",
  "heading": "*⚡ Электрика*",
  "strings": [
    {
      "topic": "/devices/ABB_EC _1/controls/value",
      "values": [
        {
          "normal_values": {
            "min_value": 2,
            "max_value": 49,
            "text": "⚡️Текущие энергопотрение: ${value} кВт/ч"
          }
        },
        {
          "trigger_value": 50,
          "action": ">",
          "text": "⚠️ *Внимание, приближение к критическому значению потребления* (${value} кВт.ч)"
        }
      ]
    }
  ]
}
```
![image](https://github.com/user-attachments/assets/52adf5a3-b99f-45ae-8932-e75af83669e8)

#### **Кнопка с инлайн-клавиатурой**
```json
{
  "row": 2,
  "order": 1,
  "name": "🔐 Безопасность",
  "heading": "🔐 *Безопасность*",
  "strings": [
    {
      "topic": "/devices/secController/controls/zone_1.01",
      "message": [
        {
          "value": 0,
          "text": "📍 Прихожая. 🔐 Дверь закрыта"
        },
        {
          "value": 1,
          "text": "📍 Прихожая. 🔓 Дверь открыта"
        }
      ]
    }
  ],
  "inline_button_id": [2, 3]
}

```
![image](https://github.com/user-attachments/assets/05cb853c-61da-49d3-890b-3748ebda4e9b)

---

### **Как это работает?**

1. **Отображение меню:**
   - При запуске бота пользователь видит первую страницу меню (`list_id: 1`).
   - Кнопки на странице позволяют переключаться между разделами или отображать информацию.

2. **Обработка нажатий:**
   - При нажатии на кнопку бот проверяет её параметры:
     - Если указан `change_list`, происходит переход на другую страницу.
     - Если указаны `strings`, бот собирает информацию из MQTT-топиков и отправляет её пользователю.

3. **Динамическое обновление данных:**
   - Бот подписывается на все указанные MQTT-топики и обновляет значения в реальном времени.
   - При изменении значения в топике бот автоматически обновляет соответствующие сообщения.

4. **Интерактивное управление:**
   - Инлайн-кнопки позволяют отправлять команды в MQTT-топики, что может использоваться для управления устройствами.

-
-

---
### **Раздел `inline_buttons`**

Раздел `inline_buttons` в конфигурации отвечает за создание интерактивных кнопок, которые отображаются в сообщениях Telegram-бота. Эти кнопки позволяют пользователям отправлять команды в MQTT-топики напрямую из чата, что делает управление системой более удобным и интуитивно понятным.

---

### **Структура раздела `inline_buttons`**

Каждый объект в массиве `inline_buttons` представляет собой одну кнопку с уникальным идентификатором (`id`). Кнопка может быть связана с определенными действиями, такими как отправка значений в MQTT-топики или отправка уведомлений пользователю.

#### **Основные параметры:**

1. **`id`**
   - **Тип:** Число
   - **Описание:** Уникальный идентификатор кнопки. Используется для привязки кнопки к конкретному сообщению.
   - **Пример:** `"id": 1`

2. **`row`**
   - **Тип:** Число
   - **Описание:** Номер строки, в которой будет размещена кнопка. Кнопки с одинаковым значением `row` будут расположены в одной строке.
   - **Пример:** `"row": 1`

3. **`order`**
   - **Тип:** Число
   - **Описание:** Порядок кнопки в строке. Кнопки с меньшим значением `order` будут располагаться левее.
   - **Пример:** `"order": 1`

4. **`text`**
   - **Тип:** Строка
   - **Описание:** Текст, который будет отображаться на кнопке.
   - **Пример:** `"text": "✅ Протечка устранена"`

5. **`message`**
   - **Тип:** Строка
   - **Описание:** всплывающие сообщение, которое будет отправлено пользователю после нажатия на кнопку. Может содержать описание выполненного действия.
   - **Пример:** `"message": "Команда на открытие кранов отправлена"`

6. **`topics`**
   - **Тип:** Массив объектов
   - **Описание:** Список MQTT-топиков и значений, которые будут отправлены при нажатии на кнопку. Каждый объект содержит:
     - **`topic`** — MQTT-топик, в который будет отправлено значение.
     - **`value`** — Значение, которое будет отправлено в топик.
   - **Пример:**
     ```json
     "topics": [
       {
         "topic": "/devices/wb-mwac-v2_11/controls/Leakage Mode/on",
         "value": 0
       },
       {
         "topic": "/devices/wb-mwac-v2_12/controls/Leakage Mode/on",
         "value": 0
       }
     ]
     ```

---

### **Как это работает?**

1. **Создание кнопок:**
   - При формировании сообщения бот проверяет, есть ли у текущего раздела или уведомления привязанные инлайн-кнопки (через параметр `inline_button_id`).
   - Если такие кнопки есть, они добавляются к сообщению в виде клавиатуры.

2. **Обработка нажатий:**
   - Когда пользователь нажимает на кнопку, бот получает её `id` через callback_data.
   - По этому `id` бот находит соответствующую кнопку в конфигурации и выполняет следующие действия:
     - Отправляет указанные значения в MQTT-топики.
     - Отправляет пользователю сообщение, указанное в параметре `message`.

3. **Отправка значений в MQTT:**
   - Для каждой записи в массиве `topics` бот публикует значение в соответствующий MQTT-топик.
   - Это позволяет управлять устройствами напрямую из Telegram.

---

### **Пример использования**

#### **Простая кнопка**
```json
{
  "id": 1,
  "row": 1,
  "order": 1,
  "text": "✅ Протечка устранена",
  "message": "Команда на открытие кранов отправлена",
  "topics": [
    {
      "topic": "/devices/wb-mwac-v2_11/controls/Leakage Mode/on",
      "value": 0
    },
    {
      "topic": "/devices/wb-mwac-v2_12/controls/Leakage Mode/on",
      "value": 0
    }
  ]
}
```
- **Функционал:** При нажатии на кнопку "✅ Протечка устранена" бот отправляет значение `0` в два MQTT-топика, что может означать отключение режима протечки.
- **Уведомление:** Пользователь получает сообщение "Команда на открытие кранов отправлена".

#### **Кнопка для управления ночным режимом**
```json
{
  "id": 2,
  "row": 1,
  "order": 1,
  "text": "🛡 Вкл. ночной режим",
  "message": "Послана команда на включение ночного режима",
  "topics": [
    {
      "topic": "/devices/bolid_convector_60/controls/section_1_write/on",
      "value": 24
    }
  ]
}
```
- **Функционал:** При нажатии на кнопку "🛡 Вкл. ночной режим" бот отправляет значение `24` в указанный MQTT-топик, что может активировать ночной режим.
- **Уведомление:** Пользователь получает сообщение "Послана команда на включение ночного режима".

#### **Кнопка для выключения ночного режима**
```json
{
  "id": 3,
  "row": 1,
  "order": 2,
  "text": "🔓 Выкл. ночной режим",
  "message": "Послана команда на выключение ночного режима",
  "topics": [
    {
      "topic": "/devices/bolid_convector_60/controls/section_1_write/on",
      "value": 109
    }
  ]
}
```
- **Функционал:** При нажатии на кнопку "🔓 Выкл. ночной режим" бот отправляет значение `109` в указанный MQTT-топик, что может деактивировать ночной режим.
- **Уведомление:** Пользователь получает сообщение "Послана команда на выключение ночного режима".

#### **Кнопка для управления охранной системой**
```json
{
  "id": 4,
  "row": 2,
  "order": 1,
  "text": "🛡 Вкл. полную охрану",
  "message": "Послана команда на включение полной охраны",
  "topics": [
    {
      "topic": "/devices/bolid_convector_60/controls/section_1_write/on",
      "value": 24
    },
    {
      "topic": "/devices/bolid_convector_60/controls/section_3_write/on",
      "value": 24
    },
    {
      "topic": "/devices/bolid_convector_60/controls/section_4_write/on",
      "value": 24
    }
  ]
}
```
- **Функционал:** При нажатии на кнопку "🛡 Вкл. полную охрану" бот отправляет значение `24` в три MQTT-топика, что может активировать режим полной охраны.
- **Уведомление:** Пользователь получает сообщение "Послана команда на включение полной охраны".

#### **Кнопка для отключения охранной системы**
```json
{
  "id": 5,
  "row": 2,
  "order": 2,
  "text": "🔓 Выкл. полную охрану",
  "message": "Послана команда на выключение полной охраны",
  "topics": [
    {
      "topic": "/devices/bolid_convector_60/controls/section_1_write/on",
      "value": 109
    },
    {
      "topic": "/devices/bolid_convector_60/controls/section_3_write/on",
      "value": 109
    },
    {
      "topic": "/devices/bolid_convector_60/controls/section_4_write/on",
      "value": 109
    }
  ]
}
```
- **Функционал:** При нажатии на кнопку "🔓 Выкл. полную охрану" бот отправляет значение `109` в три MQTT-топика, что может деактивировать режим полной охраны.
- **Уведомление:** Пользователь получает сообщение "Послана команда на выключение полной охраны".

---

## 📝 TODO

- [ ] Добавить поддержку дополнительных типов сообщений.
- [ ] Реализовать более сложную систему авторизации.
- [ ] Добавить логирование в файл.

---

## 🤝 Contributing

Pull requests приветствуются! Для крупных изменений, пожалуйста, откройте issue для обсуждения.

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
- Создайте issue в репозитории.

---

## 📈 Changelog

**v1.0.0** — релиз

---
